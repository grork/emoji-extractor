{
  "name": "emoji-extractor",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "serve": "vite",
    "clean": "rimraf output",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "watch-compile": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.1",
    "rimraf": "~6.0.1"
  }
}
